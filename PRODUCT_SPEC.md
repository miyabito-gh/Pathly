# Pathly 初期実装仕様

## 目的

明示的に登録したファイル・フォルダを、名前とタグで素早く見つけて開く。Pathly はファイラーでも、PC 全体を走査する検索インデックスでもない。

## データ

登録項目は名前、実ファイル名、保存場所、種別、タグ、メモ、お気に入り、利用回数、最終利用日時、検索対象外状態を保持する。カテゴリは1項目につき1つだけ保持し、メイン画面には常設しない。

## UI

左はアイコン中心のナビゲーション、中央は高密度の一覧、右は選択項目の詳細パネルとする。長い名前は折り返して表示し、長いパスは中間を省略してホバーで完全値を確認できる。一覧には種別、名前、タグ、短縮パス、場所を開く、その他メニューを置く。最終利用日時は詳細パネルだけに表示する。

ホームは登録済み項目全体を対象にする基本の検索画面とする。お気に入り・最近使った・よく使うは各々の項目一覧を表示し、その表示中の検索も選択中の一覧内に限定する。項目名クリックでその項目を開き、選択時の詳細表示とEnterキーで開く操作も維持する。

## データ保存先

保存先フォルダーは管理画面から変更できる。初期値は開発実行時にプロジェクトルート、ビルド版では実行ファイルと同じフォルダーとする。設定はデータベース本体とは独立したユーザー設定領域に保存する。保存先を変更する場合は現在のSQLiteデータベースを新しい場所へ複製し、元のデータベースは自動削除しない。移行先に既存のPathlyデータベースがある場合は上書きせず、変更を拒否する。

## 実装状況

登録済み項目のSQLite読込・登録・編集・削除、既定アプリで開く処理、検索と検索履歴、明示的なリンク切れ確認、重複パス警告、タグ・カテゴリ管理、データ保存先変更を実装済み。

登録画面ではファイル・フォルダー選択ダイアログまたは絶対パス入力を利用できる。一覧からの外部ファイルドラッグはTauri対応のネイティブ実装を接続済み。OS上での実動作確認はユーザーの明示指示があるまで行わない。

一覧の「その他」メニューから開く・保存場所を開く・編集・登録解除を選べる。自動ファイル走査・索引化は引き続き行わない。
