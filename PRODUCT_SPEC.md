# Pathly 製品仕様

## 目的

明示的に登録したファイル・フォルダを、名前・タグ・カテゴリで素早く見つけて開く。Pathly はファイラーでも、PC 全体を走査する検索インデックスでもない。

## データ

登録項目は名前、実ファイル名、保存場所、種別、タグ、メモ、お気に入り、利用回数、最終利用日時、検索対象外状態を保持する。カテゴリは1項目につき1つだけ保持し、メイン画面には常設しない。

## UI

左はアイコン中心のナビゲーション、中央は高密度の一覧、右は選択項目の詳細パネルとする。長い名前は折り返して表示し、長いパスは中間を省略してホバーで完全値を確認できる。一覧には種別、名前、タグ、短縮パス、場所を開く、その他メニューを置く。最終利用日時は詳細パネルだけに表示する。

ホームは登録済み項目全体を対象にする基本の検索画面とする。お気に入り・最近使った・よく使うは各々の項目一覧を表示し、その表示中の検索も選択中の一覧内に限定する。通常の検索語は名前・タグ・カテゴリを対象とする。空白区切りトークンの先頭にある`#`はタグ、`@`はカテゴリの明示絞り込みとして扱い、値は部分一致、通常語および複数の絞り込み条件はANDで組み合わせる。値に空白を含む場合は引用符で囲める。最近使ったモードは最終利用日時の新しい順を既定とし、お気に入り・よく使う・ホームは利用回数順を既定とする。検索オプションで利用回数順・最終利用日時の新しい順・名前順に変更できる。最近使ったには最終利用日時がある項目を、よく使うには利用回数が1回以上の項目を表示する。項目名クリックでその項目を開き、選択時の詳細表示とEnterキーで開く操作も維持する。

## データ保存先

保存先フォルダーは管理画面から変更できる。初期値は開発実行時にプロジェクトルート、ビルド版では実行ファイルと同じフォルダーとする。設定はデータベース本体とは独立したユーザー設定領域に保存する。保存先を変更する場合は現在のSQLiteデータベースを新しい場所へ複製し、元のデータベースは自動削除しない。移行先に既存のPathlyデータベースがある場合は上書きせず、変更を拒否する。

## 登録・管理機能

登録済み項目の読込・登録・編集・削除、既定アプリで開く処理、検索と検索履歴、明示的なリンク切れ確認、重複パス警告、タグ・カテゴリ管理、データ保存先変更を提供する。

登録画面ではファイル・フォルダー選択ダイアログ、絶対パス入力、ファイル・フォルダーのドロップを利用できる。一覧の項目は外部へドラッグできる。OS連携の実動作確認はユーザーの明示指示があるまで行わない。

通常登録と管理画面のレコード編集では、入力された絶対パスを検証して結果を表示する。存在する場合は実際の種別を使用し、存在しない場合または確認できない場合も警告とファイル／フォルダーの種別選択を伴って保存できる。リンク切れレコードのパスを変更しない編集では保存済みの種別を維持する。

管理画面のレコード機能では、SQLite内の登録レコードを一覧・検索し、許可された項目の編集と登録解除を行える。複数レコードの選択削除にも対応し、登録情報と関連タグのみを一括削除する。任意SQLやDBファイルの直接操作は提供しない。CSV入出力とExcel互換のヘッダー付きTSVコピー／貼り付けは反映前にプレビューし、ID付き行だけを更新、IDなし行を新規登録として一括検証後に単一トランザクションで反映する。パス一致による暗黙の上書きは行わない。

一覧の「その他」メニューから開く・保存場所を開く・編集・登録解除を選べる。自動ファイル走査・索引化は引き続き行わない。
