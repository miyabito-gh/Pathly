# AGENTS.md

このファイルはPathlyリポジトリで作業するAIエージェント向けの共通ルールです。製品要件の正本は`PRODUCT_SPEC.md`、利用者向け概要は`README.md`です。

## 基本方針

- ユーザーの依頼範囲を超えて、機能追加・整理・リファクタリングをしない。
- 調査・レビュー依頼では、明示的に求められない限りコードを変更しない。
- 不明点は、結果を大きく変えない範囲で最小限の仮定を置く。重要な判断が必要なら作業を広げる前に確認する。
- 作業開始時に関連ファイルと完了条件を絞り、依頼された成果ができた時点で止める。
- 既存の変更や未コミット差分をユーザーのものとして扱い、無関係な箇所を上書き・整理・コミットしない。
- シンプルで洗練されたUIを最優先する。低頻度機能をメイン画面へ常設したり、操作部品を安易に増やしたりしない。

## Pathlyの製品境界

- 対象はユーザーが明示的に登録したファイル・フォルダだけ。PC全体検索、登録フォルダ配下の自動走査、インデックス作成を追加しない。
- 基本検索は名前＋タグ。ファイル名・保存場所はユーザーが検索オプションを有効にした場合だけ検索対象にする。メモ本文は検索しない。
- カテゴリは1項目につき1つ。メイン画面に常設しない。
- メモ、お気に入り、利用回数、最終利用日時、検索対象外状態を保持する。
- ホームはお気に入り／最近使った／よく使うの切替式。検索→結果選択→Enterで開く操作を優先する。
- 詳細は項目選択時に右パネルへ表示し、管理機能は別画面に置く。
- リンク切れ確認は自動実行せず、管理画面から明示的に実行する。
- 重複パスの登録は許可し、登録時に注意を表示する。
- SQLiteを初期DBとする。UIや業務ロジックをSQLiteへ直結させず、将来のDB切替が可能なデータアクセス境界を保つ。
- 現在の技術構成はTauri 2 / Rust 2021 / Svelte 5 / TypeScript / Vite。MeetDockと同じ言語・デスクトップ基盤を使うが、Pathlyに不要なMeetDock固有のアーキテクチャや機能を持ち込まない。

## 作業の進め方

1. 依頼を調査・設計・実装・テスト・文書更新に分け、実装の要否を確認する。
2. まず対象箇所と既存の設計・テストを必要な範囲だけ読む。
3. 変更は小さく保ち、既存の構成・命名・責務に合わせて実装する。大規模な抽象化や先回りの拡張を避ける。
4. 変更箇所に対応する最小のテスト／チェックを実行する。成功後に必要性がある場合だけ広いテストやビルドを行う。
5. 最後に変更ファイルと差分を確認し、実施した確認と未解決事項を簡潔に報告する。

## 探索範囲

- まず`src/`、`src-tauri/src/`、`src/lib/`内の関連ファイルと関連テストを調べる。
- `node_modules/`、`build/`、`.svelte-kit/`、`src-tauri/target/`、生成物は明示的な必要がない限り検索しない。
- テストや検索では`rg`を優先し、生成物・依存ディレクトリを除外する。
- `PRODUCT_SPEC.md`や他の設計文書は、依頼に関係する章だけ読む。

## 実装上の境界

- フロントエンドは表示と入力を扱い、ファイル操作・永続化・OS操作を直接実装しない。必要な処理は狭いTauri IPC経由でRustへ委譲する。
- Tauri IPCには必要最小限の型付きデータだけを渡し、呼出し側から受け取った任意パスを無検証で実行・削除・書換えしない。
- ファイルの起動、保存場所を開く、ドラッグなどのOS連携はRust側で対象と操作を検証して実行する。
- 検索は登録済みレコードのメタデータだけに対して行う。実ファイル内容の走査やバックグラウンド索引化を実装しない。
- SQLite固有SQL・接続管理はリポジトリ実装へ閉じ込め、画面やドメイン型に漏らさない。
- 権限、CSP、ファイルアクセス範囲は必要最小限とし、任意コマンド実行や不要なプラグイン権限を追加しない。
- UIでは視覚順とキーボード移動順を一致させ、明確なフォーカス表示、アクセシブルな名称、読み上げ可能な状態を保つ。

## AIチケット消費・モデル利用

- Astra（`gpt-6-astra`）は既定で使わない。ユーザーが明示的に指定した場合、または複数領域にまたがる高難度・高リスクの作業で、より軽量なモデルでは品質要件を満たせないと判断した場合に限る。
- Astraを使う必要があるか迷う場合は使わず、現在選択可能な軽量・低コストモデルで進める。モデル名は環境で選択可能なものを優先し、AGENTS内で特定モデルを固定しない。
- チケット消費を抑えるため、依頼を小さく区切り、探索対象・読み込む文書・テストを限定する。不要な再調査、重複した説明、網羅目的だけの全体テスト、追加エージェントへの分割を避ける。
- サブタスク分割は並列作業が実際に時間・品質を改善し、調整コストを上回る場合に限る。
- モデル比較や推奨をユーザーから求められたときは、必要な場合に限り公式情報で現行の選択肢を確認する。通常のPathly実装でモデル調査を追加しない。

## テストと開発コマンド

- フロントエンド型・アクセシビリティ確認: `npm run check`
- フロントエンドテスト: `npm test`
- WebView向け本番ビルド: `npm run build`
- Rust確認: `cargo check --manifest-path src-tauri/Cargo.toml`
- Rustテスト: `cargo test --manifest-path src-tauri/Cargo.toml`
- 変更に関係するものを優先し、依存取得が必要な場合は承認済みの方法で行う。
- ビルド成功だけでWindows/WebView2実機動作を確認済みと扱わない。手動確認が必要なら未実施として区別する。

## Git操作

- 読み取り操作で作業ツリー、差分、ブランチを確認してから変更する。
- ユーザーが頼んでいない`git add`、`commit`、`push`、ブランチ操作を行わない。
- コミットを依頼された場合は、対象ファイルと差分を確認してから、依頼範囲のファイルだけをステージする。
- 既存履歴を書き換える操作や破壊的なGit操作は、明確な依頼なしに実行しない。

## 引継ぎ

- 引継ぎ依頼では、目的、完了条件、対象ファイル、実装済み事項、実施済み確認、未解決事項を具体的にまとめる。
- 単一の小さな作業は同じタスク内で継続し、調査・設計・実装など独立性の高い大きな工程だけ分割する。
- モデル推奨はユーザーが求める場合に限り、Astraを自動的に既定候補にしない。

## 最終報告

- 変更ファイル、実施したテスト／確認、未解決事項を中心に簡潔に報告する。
- 失敗した確認は理由を添えて成功と区別し、実機未確認事項を隠さない。
- 作業中にスキルや外部情報が重要な判断へ影響した場合は、その要点を簡潔に記載する。
