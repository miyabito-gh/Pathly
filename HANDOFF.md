# Pathly 引継ぎメモ

更新日: 2026-09-22

## 次に対応すること

ユーザーはコンテキストを切り替えて作業を続けたい。まずこのメモと `AGENTS.md`、`PRODUCT_SPEC.md` を確認し、当初要件を保ったまま以下の未解決事項を優先して調査・修正する。Astraは使わず、不要なサブエージェント分割もしない。

1. **保存パスからWindows拡張形式を除く** — 登録・編集時に入力正規化を追加したが、ユーザーのDBには依然 `\\?\C:\Users\wmasa\Documents\Rust\MeetDock` が保存されている。`std::fs::canonicalize` がWindowsで拡張形式を返して再保存している可能性を調べる。表示だけでなくSQLiteの既存値と新規登録値を通常の絶対パス形式にし、UNC (`\\server\share`) を壊さない。既存DBを安全に移行する方法も検討し、登録先の実在性・絶対パス検証は維持する。
2. **項目を開く操作を要件どおり明確にする** — 現行の `PRODUCT_SPEC.md` / `AGENTS.md` は「検索→結果選択→Enterで開く」と定める。現在、名前ボタンのクリックは選択のみで、開く処理はEnter、詳細パネルの「開く」、その他メニューにある。まず最初の要件・この仕様を正本として扱い、名前クリックで開く追加要望と矛盾しない操作にする（クリックで選択を保つなら、視認できる開く操作・Enter動作を確実にする）。ユーザーの「名前をクリックしても開かない」を放置せず、仕様と実機の操作を突き合わせて判断を記録する。
3. **ホームボタンの役割を解消** — 現状 `goHome()` は管理画面を閉じ、最近使った表示へ戻す。一方、最近使ったボタンも同じ表示に遷移するため、ホームの意味が重複している。3つのホーム表示切替（お気に入り／最近使った／よく使う）を主とし、ホームは初期表示へ戻す役割として必要か判断する。不要ならナビから除く。意味が明確でない新ボタンや説明文で補わない。
4. **レイアウトを初期要件に合わせて整理** — アイコン中心の左ナビ・高密度の中央一覧・右詳細パネルを維持し、簡潔で使いやすい見た目にする。ユーザー要望により、説明目的の文言（例: 画面上部のキャッチコピー、操作説明、管理画面の補足説明）は削り、機能ラベル・エラー・状態表示など必要な文言だけ残す。低頻度機能をメイン画面に増やさない。実機UI確認はユーザーが指示するまで行わない。

## 初期要件（必ず維持）

- Pathlyはユーザーが明示登録したファイル／フォルダーだけを扱う。PC全体検索、登録フォルダー配下の自動走査、内容索引化をしない。
- 基本検索は名前＋タグ。実ファイル名・保存場所は検索オプション時のみ。メモ本文は検索しない。
- SQLiteを初期DBとし、UI・業務ロジックからDB実装を分離する。
- 名前、実ファイル名、パス、種別、タグ、メモ、お気に入り、利用回数、最終利用日時、検索対象外状態を保持。カテゴリは1項目につき1つでメイン画面には常設しない。
- 初期画面はお気に入り／最近使った／よく使うの切替式。検索して結果を選択しEnterで開く。項目選択時は右詳細パネルを表示する。
- 左はアイコン中心のナビ、中央は高密度一覧。種別・名前・タグ・短縮パス・場所を開く・その他メニューを配置する。最終利用日時は詳細に置く。長い名前は折り返し、長いパスは省略表示＋ホバーで完全値を確認する。
- 管理機能は別画面。リンク切れ検査はユーザーが明示的に実行する。重複パスは警告後も登録可能。
- 保存先は変更可能。devの既定はプロジェクトルート、buildの既定はexeと同じ場所。保存先変更ではDBを複製し、元DBは自動削除しない。
- 技術構成: Tauri 2 / Rust 2021 / Svelte 5 / TypeScript / Vite。Astraを既定使用しない。

## 実装済みとして確認できる範囲

- SQLiteリポジトリを介した登録・一覧・編集・削除、既定アプリで開く、検索履歴、明示的リンク検査、重複警告、タグ・カテゴリ管理、保存先変更がある。
- 登録・編集コマンドは `\\?\` / `\\?\UNC\` 入力を処理するが、ユーザー報告ではDB保存値にプレフィックスが残る。修正完了とは扱わない。
- `run.ps1` に `dev` / `build` オプションを統合。ASCIIのみの現行ファイルは当該PowerShell環境で構文解析成功。ただしユーザー環境での再実行結果は未確認。`run-dev.ps1` / `run-build.ps1` は削除済み。
- UIのネイティブドロップ登録と、一覧から外部へドラッグする実装がある。OS実機での確認は未実施。

## 直近の確認と制約

- 直近実装時: `cargo test --manifest-path src-tauri/Cargo.toml` 12件成功、`npm.cmd run check` 0 error / 0 warning、`npm.cmd test` 1件成功、`git diff --check` 成功。
- これらは前回変更時の結果。次の修正後は変更に関連する最小のテストを再実行する。
- ユーザーの明示指示があるまでアプリを起動したり、OS上でドラッグを含む自動・手動実機確認をしたりしない。
- Gitの作業ツリーには既存の広範な未コミット変更がある。コミット／プッシュ、無関係な整理、ユーザーのDB削除はしない。
- `pathly.sqlite3` が未追跡で存在する。ユーザーデータとして扱い、内容を不用意に変更・削除しない。

## 主な対象ファイル

- `PRODUCT_SPEC.md`, `AGENTS.md`: 要件と作業ルール
- `src/routes/+page.svelte`, `src/app.css`: 画面・操作・レイアウト
- `src-tauri/src/lib.rs`: Windowsパス正規化、IPC、DBリポジトリ、OS起動処理
- `run.ps1`: dev/build起動

## 完了条件

- 新規登録・編集後のパスがSQLiteにも通常のWindows絶対パスとして保存され、既存レコードの対処が決まっている。
- 「選択」「開く」「ホーム／3表示切替」の挙動が初期要件と矛盾せず、操作意図が分かる。
- レイアウトが初期要件の3ペイン構成を保ち、不要な説明文がなく、必要な操作・状態表示は残る。
- 対応するコード検査／テスト結果と、未実施の実機確認が区別して記録される。
