# 修正点・改善点一覧

このドキュメントは、作業中に受け取った修正点・改善点を入力順に記録します。

## 1. メモ欄の改行が詳細ペインに反映されない

- 編集時にメモ欄へ改行を含めて保存しても、詳細ペインでは改行が反映されず、連続した文字列として表示される。

## 2. パスとして成立していない文字列を登録できる種類の追加

- 現在のパスとして成立していない文字列でも登録できるようにする。
- そのための専用の種類を追加する。
- 登録画面から専用の種類を選択し、任意の文字列を登録できるようにする。

## 3. モーダル画面外クリックによる意図しない閉じ動作の修正

- モーダル表示中にモーダル画面外をクリックすると、すぐにモーダルが閉じてしまう。
- 画面外クリック時に意図せず閉じないように修正する。

## 4. 利用回数を編集可能にする

- 登録済み項目の利用回数を編集できるようにする。

## 5. パスとして成立していない文字列用の専用アイコン追加

- パスとして成立していない文字列を登録する種類について、通常のパスとは異なる専用アイコンを表示する。
